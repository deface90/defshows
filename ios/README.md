# defShows для iPhone

Нативный клиент на SwiftUI для iOS 17+. Без сторонних зависимостей.
Нижние вкладки:

- **Мои сериалы**: коллекция, поиск по ней, сезоны и отметки просмотра.
- **Поиск**: поиск TMDB, карточка сериала и добавление в коллекцию.
- **Подбор**: жанр, страна, минимальный рейтинг, сортировка и следующие страницы.
- **Уведомления**: лента, отметка одного или всех уведомлений прочитанными.
- **Ещё**: поиск пользователей, публичные коллекции, настройки профиля и уведомлений, выход.

Карточки из коллекции и каталога показывают постер, оригинальное название, год,
жанры, статус выхода, дату следующей серии, рейтинги TMDB и других источников,
описание и ссылки IMDb/Wikipedia. У серий отображаются код, дата, длительность
и рейтинг; у сезонов — рейтинг и прогресс для коллекции.

В своей коллекции можно менять статус сериала, отмечать следующую серию,
вышедшие серии сезона или весь сериал (последнее включает все серии в каталоге
и требует подтверждения). По умолчанию раскрыт сезон с самой ранней непросмотренной
серией (той же, что в «Продолжить просмотр»); если всё вышедшее просмотрено —
последний сезон.
Приватные заметки доступны из карточки: создание для сериала/сезона/серии,
редактирование текста и удаление с подтверждением.

Сезоны в карточках расположены от последнего к первому; серии — по возрастанию номера.

Приложение подключается к `https://shows.deface.dev/api`; ввод сервера не нужен.
Ссылка на политику конфиденциальности доступна на экране входа и в «Ещё → О сервисе»
и открывает `https://shows.deface.dev/privacy` в браузере.
Токены сохраняются в Keychain; при HTTP 401
клиент обновляет токены и повторяет запрос один раз.

## Запуск на Mac

1. Установи Xcode с поддержкой iOS 17 или новее и открой `DefShows.xcodeproj`.
2. Для симулятора выбери iPhone в списке устройств и нажми Run (⌘R).
3. Для своего iPhone: в Xcode → Settings → Accounts добавь Apple Account.
   В target DefShows → Signing & Capabilities выбери свою Personal Team.
   Если стандартный Bundle Identifier недоступен, замени `com.defshows.ios`
   на уникальный, например `com.yourname.defshows`.
4. Подключи iPhone, включи Developer Mode, если Xcode предложит,
   выбери телефон в списке устройств и нажми Run.
5. Войди существующим email и паролем от shows.deface.dev.
   Новый аккаунт можно создать с экрана входа. Социальный вход и сброс
   пароля в этой версии не реализованы. Для аккаунта только с OAuth сначала
   настрой пароль через веб-версию, если она это позволяет.

Разрабатывать и запускать на личном устройстве можно с бесплатной Personal Team.
Push-уведомления (capability Push Notifications, `DefShows.entitlements`) требуют
платного Apple Developer аккаунта и APNs-ключа — см. ниже.

## Push-уведомления

Приложение регистрирует APNs device token после входа (запрашивает разрешение,
шлёт токен на `POST /me/device-token`, снимает его на `DELETE /me/device-token`
при выходе) и открывает карточку сериала по тапу на пуш. Присылает те же три
события, что и Telegram-бот: вышла серия / скоро серия / скоро сезон — независимый
канал, можно получать оба одновременно.

На сервере нужны переменные `APNS_TEAM_ID`, `APNS_KEY_ID`, `APNS_KEY` (содержимое
`.p8`-ключа), `APNS_TOPIC` (`com.defshows.ios`), `APNS_PRODUCTION` — см.
`backend/deploy/.env.example`. Ключ создаётся в developer.apple.com →
Certificates, Identifiers & Profiles → Keys → APNs (скачивается один раз).
Без этих переменных `cmd/notifier` просто не запускает push-отправку (лог
`APNS_TEAM_ID/APNS_KEY_ID/APNS_KEY not set`), остальное продолжает работать.

Тестирование без реального APNs: перетащи `.apns`-JSON с `aps.alert` в окно
симулятора (Xcode → Simulator принимает файл как push-уведомление) — проверяет
обработку баннера и переход в карточку сериала без похода на сервер.
Платная подписка нужна позже для TestFlight / App Store.

## Проверка сборки

На Mac из корня репозитория:

```sh
xcodebuild -project ios/DefShows.xcodeproj -scheme DefShows \
  -configuration Debug -sdk iphonesimulator \
  -destination 'generic/platform=iOS Simulator' \
  -derivedDataPath /tmp/defshows-ios-build CODE_SIGNING_ALLOWED=NO build
```

Тесты декодирования API и порядка сезонов (на Mac):

```sh
cd ios
swift test
```

Ручная проверка на тестовом аккаунте:

- Войти с неверным паролем: видна ошибка, можно повторить вход.
- Войти с верным паролем: список совпадает с веб-версией.
- Открыть сериал, отметить серию, проверить отметку в веб-версии, отменить её.
- Вернуться в коллекцию: прогресс обновляется; потянуть список вниз для обновления.
- Перезапустить приложение: сессия восстанавливается из Keychain.
- После истечения access-токена обновить список: сессия продлевается автоматически.
- Отключить сеть: появляется ошибка, отметка серии не меняется без подтверждения.
- Найти сериал с кириллицей и знаком `&`, открыть карточку и добавить в коллекцию.
- Переключиться на «Мои сериалы»: добавленный сериал появился.
- В подборе выбрать фильтры, загрузить следующую страницу, открыть сериал.
- Проверить постер, название, жанры, рейтинги и ссылки в карточках коллекции и каталога.
- Сменить статус, отметить следующую серию и сезон, проверить изменения в веб-версии.
- При отключении сети во время отметки сезона проверить сохранение успешных отметок после обновления.
- Создать заметки для сериала, сезона и серии; изменить текст и удалить заметку.
- Проверить порядок сезонов: последний первым, серии внутри — по возрастанию.
- Открыть сериал с недосмотренным средним сезоном: раскрыт должен быть именно он.
- Открыть публичную коллекцию пользователя; закрытый профиль показывает понятное состояние.
- Отметить уведомление прочитанным и проверить состояние в веб-версии.
- Изменить настройки, сохранить и проверить их после повторного открытия.
- Выйти и перезапустить: снова виден экран входа. Для отзыва сессии нужна сеть.

## Что ещё не перенесено

Главная с рекомендациями, непросмотренные серии отдельным списком, избранное и удаление
сериала из коллекции, админка, привязка Telegram, смена пароля и OAuth
пока остаются в веб-версии.
Клиент требует подключения к серверу; офлайн-кэш не реализован.
Иконка из корневого `logo.png` подключена через `Assets.xcassets/AppIcon.appiconset`.
Она экспортирована в 1024×1024 RGB с чёрным фоном без прозрачности.
Перед App Store ещё нужны материалы публикации и проверка требований магазина.

В Linux-среде разработки нет Xcode / iOS SDK: сборку и поведение на устройстве
необходимо проверить на Mac командой выше. Backend и его контракты не менялись.
